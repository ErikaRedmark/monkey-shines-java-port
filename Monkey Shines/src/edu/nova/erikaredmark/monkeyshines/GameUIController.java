package edu.nova.erikaredmark.monkeyshines;

/**
 * 
 * Acts as a backing controller to the main UI panel on the main game window. The
 * UI draws, the World contains stateful data that is reflected on the UI. This class
 * handles converting that data to drawing information for display. For example, the
 * current powerup has to be drawn in a specific place, and will flash in and out when
 * the powerup is fading (TODO note that not all game engine aspects are implemented
 * yet).
 * 
 */
public final class GameUIController {

	
	
}
